# simple_answer.yml
# The provided question/answer yml example string contains 2 fields (user_prompt, answer).

# meta: {"name": "user_prompt", "type": "string", "default": "", "example": "Hello, Who are you?"}
# user_prompt contains the question/problem statement that founds/initiates the thought/chat
user_prompt: null

# meta: {"name": "answer", "type": "string", "default": "", "example": "I am a helpful assistant. How can I help you today?"}
# The answer field contains the model response
answer: null

